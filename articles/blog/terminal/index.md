# 终端 命令行

就是喜欢终端，命令行操作真的爽。

想要一份配置就能快速还原我喜欢的操作环境，所以配置了
[dotfiles 仓库](https://github.com/hf-xz/dotfiles)。

最近在服务器上操作，服务器上没有 zsh，导致我陪着的这么多
zsh 配置都用不了。考虑换一个跨平台的 Shell。

最终选定了 `fish` + `starship` 的组和。

详细的配置我都记录到 dotfiles 仓库里了。
